import { Canvas, Meta, Story } from "@storybook/addon-docs";
import MyButton from "../src/Button.vue";
import { Chart } from "./chart";
import { Prototype } from "./prototype";
import dedent from "ts-dedent";
import {
  Title,
  Subtitle,
  Description,
  Primary,
  ArgsTable,
  Stories,
  PRIMARY_STORY,
  Source,
} from "@storybook/addon-docs";

<Meta
  title="sse/ReviseRequistionReviseRecordTable"
  component={MyButton}
  parameters={{
    docs: {
      description: {
        component: "_description_",
      },
    },
  }}
  argTypes={{
    backgroundColor: { control: "color" },
    onClick: {},
    size: {
      control: { type: "select" },
      options: ["small", "medium", "large"],
    },
  }}
/>

export const Template = (args) => ({
  components: { MyButton },
  setup() {
    return { args };
  },
  template: '<div><my-button v-bind="args" /></div>',
});

# 申请单中的修订记录表

<Description of={"."} />

<div className="subheading">原型</div>

<Prototype />

<div className="subheading">目的</div>

- 显示修订记录
- 撤销某一条修订

<div className="subheading">用例</div>

- 修订结果: `reviseRequistionReviseRecord`
- (click: 选中字段、click: 选中字段表头)记录为 batchFlagIds
- (click: "移出选中"按钮)打开确认弹框
- (click: 确认弹框中的"返回"按钮)关闭确认弹框
- (click: 确认弹框中的"确认"按钮)提交撤销、关闭确认弹框: `delete(batchFlagIds)`
- (click: "提交审核"按钮)提交草稿申请单至审核: `commitToReview()`

<div className="subheading">实现</div>

<Canvas columns={5}>
  <Story name="Secondary" args={{ label: "secondary" }}>
    {Template.bind({})}
  </Story>
  <Story name="Large" args={{ size: "large", label: "Button" }}>
    {Template.bind({})}
  </Story>
</Canvas>

<!--<div className="subheading">使用示例</div>
<Source dark id="." />

<div className="subheading">状态图、类图、序列图等</div>

<Chart />-->

<style>
  {`
    .subheading {
      --mediumdark: '#999999';
      font-weight: 900;
      font-size: 16px;
      color: #999;
      letter-spacing: 6px;
      line-height: 24px;
      text-transform: uppercase;
      margin-bottom: 12px;
      margin-top: 40px;
    }
`}
</style>
