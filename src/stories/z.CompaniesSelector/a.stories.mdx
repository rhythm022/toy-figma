import { Canvas, Meta, Story } from "@storybook/addon-docs";
import MyButton from "../src/Button.vue";
import { Chart } from "./chart";
import { Prototype } from "./prototype";
import dedent from "ts-dedent";
import {
  Title,
  Subtitle,
  Description,
  Primary,
  ArgsTable,
  Stories,
  PRIMARY_STORY,
  Source,
} from "@storybook/addon-docs";

<Meta
  title="sse/CompaniesSelector"
  component={MyButton}
  parameters={{
    docs: {
      description: {
        component: "_description_",
      },
    },
  }}
  argTypes={{
    backgroundColor: { control: "color" },
    onClick: {},
    size: {
      control: { type: "select" },
      options: ["small", "medium", "large"],
    },
  }}
/>

export const Template = (args) => ({
  components: { MyButton },
  setup() {
    return { args };
  },
  template: '<div><my-button v-bind="args" /></div>',
});

# 公司选择器

<Description of={"."} />

<Prototype />

<div className="subheading">目的</div>

- 选择公司

<div className="subheading">途径/形式</div>
<div className="subheading">用例信息</div>

- 全部公司
- 被选中的公司

<div className="subheading">用例</div>

- 全部公司及其是否被选中: `companySelecting.companies`
- (方法)改变公司的选中状态: `companySelecting.toggleSelecting(company,newVal)`

<div className="subheading">实现</div>

<Canvas columns={5}>
  <Story name="Secondary" args={{ label: "secondary" }}>
    {Template.bind({})}
  </Story>
  <Story name="Large" args={{ size: "large", label: "Button" }}>
    {Template.bind({})}
  </Story>
</Canvas>

<!--<div className="subheading">使用示例</div>
<Source dark id="." />

<div className="subheading">状态图、类图、序列图等</div>

<Chart />-->

<style>
  {`
    .subheading {
      --mediumdark: '#999999';
      font-weight: 900;
      font-size: 16px;
      color: #999;
      letter-spacing: 6px;
      line-height: 24px;
      text-transform: uppercase;
      margin-bottom: 12px;
      margin-top: 40px;
    }
`}
</style>
