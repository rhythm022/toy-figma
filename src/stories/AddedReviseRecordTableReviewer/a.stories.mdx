import { Canvas, Meta, Story } from "@storybook/addon-docs";
import MyButton from "../src/Button.vue";
import { Chart } from "./chart";
import { Prototype } from "./prototype";
import dedent from "ts-dedent";
import {
  Title,
  Subtitle,
  Description,
  Primary,
  ArgsTable,
  Stories,
  PRIMARY_STORY,
  Source,
} from "@storybook/addon-docs";

<Meta
  title="sse/AddedReviseRecordTableReviewer"
  component={MyButton}
  parameters={{
    docs: {
      description: {
        component: "_description_",
      },
    },
  }}
  argTypes={{
    backgroundColor: { control: "color" },
    onClick: {},
    size: {
      control: { type: "select" },
      options: ["small", "medium", "large"],
    },
  }}
/>

export const Template = (args) => ({
  components: { MyButton },
  setup() {
    return { args };
  },
  template: '<div><my-button v-bind="args" /></div>',
});

# 申请单审核

<Description of={"."} />

<Prototype />

<div className="subheading">目的</div>

- 收集用户对申请单内修订结果的审核意见

<div className="subheading">途径</div>

- 逐个修改单元格(审核意见)的值(checkbox)
- 逐个修改单元格(填写描述)的值(input)
- 选择(全选字段)后批量操作

<div className="subheading">依赖于</div>
<div className="subheading">下层依赖</div>

- 申请单及其审核意见：`addedReviseRecordTableReviewing.items`
- （方法）改变审核意见:`addedReviseRecordTableReviewing.setItem(id,key,newVal)`
- （方法）提交:`addedReviseRecordTableReviewing.reviewDone()`

<div className="subheading">上层依赖</div>
<div className="subheading">实现</div>

<Canvas columns={5}>
  <Story name="Secondary" args={{ label: "secondary" }}>
    {Template.bind({})}
  </Story>
  <Story name="Large" args={{ size: "large", label: "Button" }}>
    {Template.bind({})}
  </Story>
</Canvas>

<!--<div className="subheading">使用示例</div>
<Source dark id="." />

<div className="subheading">状态图、类图、序列图等</div>

<Chart />-->

<style>
  {`
    .subheading {
      --mediumdark: '#999999';
      font-weight: 900;
      font-size: 16px;
      color: #999;
      letter-spacing: 6px;
      line-height: 24px;
      text-transform: uppercase;
      margin-bottom: 12px;
      margin-top: 40px;
    }
`}
</style>
