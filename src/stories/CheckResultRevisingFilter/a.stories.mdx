import { Canvas, Meta, Story } from "@storybook/addon-docs";
import MyButton from "../src/Button.vue";
import { Chart } from "./chart";
import { Prototype } from "./prototype";
import dedent from "ts-dedent";
import {
  Title,
  Subtitle,
  Description,
  Primary,
  ArgsTable,
  Stories,
  PRIMARY_STORY,
  Source,
} from "@storybook/addon-docs";

<Meta
  title="sse/CheckResultRevisingFilter"
  component={MyButton}
  parameters={{
    docs: {
      description: {
        component: "_description_",
      },
    },
  }}
  argTypes={{
    backgroundColor: { control: "color" },
    onClick: {},
    size: {
      control: { type: "select" },
      options: ["small", "medium", "large"],
    },
  }}
/>

export const Template = (args) => ({
  components: { MyButton },
  setup() {
    return { args };
  },
  template: '<div><my-button v-bind="args" /></div>',
});

# 校核结果的过滤项收集器

<Description of={"."} />

<Prototype />

<div className="subheading">目的</div>

收集用于过滤修订中校核结果的过滤项

<div className="subheading">途径</div>

- 提供匹配项目进行选择（selector）
- 直接输入（input）

<div className="subheading">依赖于</div>

- 候选过滤项
- 被收集的过滤项

<div className="subheading">下层依赖</div>

- 过滤项：`checkResultRevisingFilter.items`
- （方法）计算候选过滤项：`checkResultRevisingFilter.getItemOptions(key,condition)`
- （方法）改变过滤项：`checkResultRevisingFilter.setItem(key,newVal)`
- （方法）提交过滤：`checkResultRevisingFilter.filter()`

<div className="subheading">上层依赖</div>

无

<div className="subheading">实现</div>

<Canvas columns={5}>
  <Story name="Secondary" args={{ label: "secondary" }}>
    {Template.bind({})}
  </Story>
  <Story name="Large" args={{ size: "large", label: "Button" }}>
    {Template.bind({})}
  </Story>
</Canvas>

<!--<div className="subheading">使用示例</div>
<Source dark id="." />

<div className="subheading">状态图、类图、序列图等</div>

<Chart />-->

<style>
  {`
    .subheading {
      --mediumdark: '#999999';
      font-weight: 900;
      font-size: 16px;
      color: #999;
      letter-spacing: 6px;
      line-height: 24px;
      text-transform: uppercase;
      margin-bottom: 12px;
      margin-top: 40px;
    }
`}
</style>
