import { Canvas, Meta, Story } from "@storybook/addon-docs";
import MyButton from "./Button.vue";
import { Docs } from "./Button.docs";
import { Prototype } from "./Button.prototype";
import dedent from "ts-dedent";
import {
  Title,
  Subtitle,
  Description,
  Primary,
  ArgsTable,
  Stories,
  PRIMARY_STORY,
  Source,
} from "@storybook/addon-docs";

<Meta
  title="sse/MyButton"
  component={MyButton}
  parameters={{
    docs: {
      description: {
        component: "_description_",
      },
    },
  }}
  argTypes={{
    backgroundColor: { control: "color" },
    onClick: {},
    size: {
      control: { type: "select" },
      options: ["small", "medium", "large"],
    },
  }}
/>

export const Template = (args) => ({
  components: { MyButton },
  setup() {
    return { args };
  },
  template: '<div><my-button v-bind="args" /></div>',
});

# 提单过滤器

<Description of={"."} />

<Canvas columns={5}>
  <Story name="Secondary" args={{ label: "secondary" }}>
    {Template.bind({})}
  </Story>
  <Story name="Large" args={{ size: "large", label: "Button" }}>
    {Template.bind({})}
  </Story>
  <Story name="Small" args={{ size: "small", label: "Button" }}>
    {Template.bind({})}
  </Story>
</Canvas>

### 使用示例

<Source dark id="sse-mybutton--secondary" />

### 目的

收集信息（达到某一状态时进行快照，提交快照）

### 途径

- 提供可选项目进行选择（selector）
- 直接输入（input）

### 依赖于

- 可选项目

### context

- 纯属性：无
- 纯方法：提交快照
- 属性及其方法：可选项目（提交请求、数据）

### 原型

<Prototype />

### 实现(状态图、类图、序列图等)

<Docs />
