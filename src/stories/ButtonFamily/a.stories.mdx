import { Canvas, Meta, Story } from "@storybook/addon-docs";
import MyButton from "../src/Button.vue";
import { Chart } from "./chart";
import { Prototype } from "./prototype";
import dedent from "ts-dedent";
import {
  Title,
  Subtitle,
  Description,
  Primary,
  ArgsTable,
  Stories,
  PRIMARY_STORY,
  Source,
} from "@storybook/addon-docs";

<Meta
  title="sse/ButtonFamily"
  component={MyButton}
  parameters={{
    docs: {
      description: {
        component: "_description_",
      },
    },
  }}
  argTypes={{
    backgroundColor: { control: "color" },
    onClick: {},
    size: {
      control: { type: "select" },
      options: ["small", "medium", "large"],
    },
  }}
/>

export const Template = (args) => ({
  components: { MyButton },
  setup() {
    return { args };
  },
  template: '<div><my-button v-bind="args" /></div>',
});

# 操作按钮类

<Description of={"."} />

<Prototype />

<div className="subheading">目的</div>

<div className="subheading">用例</div>

- 修订记录数量: `draftReviseRecord.length`
- (click: "添加至草稿申请单()"按钮)提交修订记录至草稿申请单: `commitToDraftRequistion()`
- (click: "提交审核"按钮)提交草稿申请单至审核: `commitToReview()`
- (click: "再次提交审核()"按钮)提交修订记录直接至审核: `commitToDirectlyReview()`

- (click: "完毕审核"按钮)提交: `reviewDone()`

<div className="subheading">实现</div>

<Canvas columns={5}>
  <Story name="Secondary" args={{ label: "secondary" }}>
    {Template.bind({})}
  </Story>
  <Story name="Large" args={{ size: "large", label: "Button" }}>
    {Template.bind({})}
  </Story>
</Canvas>

<!--<div className="subheading">使用示例</div>
<Source dark id="." />

<div className="subheading">状态图、类图、序列图等</div>

<Chart />-->

<style>
  {`
    .subheading {
      --mediumdark: '#999999';
      font-weight: 900;
      font-size: 16px;
      color: #999;
      letter-spacing: 6px;
      line-height: 24px;
      text-transform: uppercase;
      margin-bottom: 12px;
      margin-top: 40px;
    }
`}
</style>
